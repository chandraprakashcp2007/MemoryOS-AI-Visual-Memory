# Local development only.  In Vercel set this to the HTTPS URL of the
# separately deployed MemoryOS API, for example https://api.memoryos.example
VITE_API_BASE_URL=http://127.0.0.1:8000
# Set true only when the deployed API has MEMORYOS_CLOUD_MODE=true.
VITE_CLOUD_AUTH=false
